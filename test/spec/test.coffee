###
 * Created with WebStorm.
 * User: nickbespalov
 * Date: 02.01.14
 * Time: 2:39
###


define [
  'backbone'
],
(Backbone) ->

  describe 'Vent initialize',
  ->
    sut = null

    beforeEach ->
      sut = true
      return

    afterEach ->
      sut = null
      return

    it 'should be defined',
    ->
      expect(sut).to.be.not.undefined
      return

    return
  return

